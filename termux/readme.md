# Termux SSH Key Bootstrap

Run this on your Termux phone to install SSH tooling, generate a keypair, and print the public key for registration on `hal`.

```bash
curl -fsSL https://raw.githubusercontent.com/jgumbley/setup-system/main/termux/init.sh | bash
```
